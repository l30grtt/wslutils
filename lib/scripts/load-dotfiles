#!/bin/sh
CONFIG=$1
. $CONFIG

### LOAD PROFILE
touch $PROFILE_TARGET_PATH
cat $PROFILE_BASE_PATH >> $PROFILE_TARGET_PATH

### LOAD ALIASES
touch $ALIAS_TARGET_PATH
cat $ALIAS_BASE_PATH   >> $ALIAS_TARGET_PATH

### LOAD GIT CONFIG
touch $GC_TARGET_PATH
cat $GC_BASE_PATH      >> $GC_TARGET_PATH
